"""q23.data.providers

Concrete DataProvider adapters. Import the specific provider you need
(e.g. `from q23.data.providers.quantiacs import QuantiacsProvider`) rather
than importing this package wholesale, since each adapter has its own
optional dependency (qnt, requests, ...).
"""
